version: "3.9"

services:
  maps-unimetatech:
    build: .
    container_name: maps-unimetatech
    image: node:18-alpine
    working_dir: /app
    ports:
      - "5173:5173"   
    volumes:
      - .:/app        
      - /app/node_modules
    command: npm run dev -- --host
